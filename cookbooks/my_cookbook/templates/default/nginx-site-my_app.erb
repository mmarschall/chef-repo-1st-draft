server {
  listen 80;
  server_name _;
  root <%= @app_home %>/public;

  # set far future expires header for static resources
  location ~* \.(ico|css|js|gif|jp?g|png|swf) {
    if ($args ~ [0-9]+) {
      expires max;
      break;
    }
  }

  location / {
    try_files $uri $uri/ =404;
  }
}